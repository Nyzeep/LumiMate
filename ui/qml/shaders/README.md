ShaderEffect sources are optional in v1.

If compiled `.qsb` files are present here, `ShaderHost.qml` will load them.
If they are missing, the QML fallback background remains active so LumiMate still starts.
