# AGENTS.md

## Agent skills

### Issue tracker

Issues are tracked as local markdown files under `.scratch/`；当前 effort `agent-upgrade` 的 ticket 位于 `.scratch/agent-upgrade/issues/`。See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `doc/CONTEXT.md` + `doc/adr/`. See `docs/agents/domain.md`.
